# Sonorous — Phase 1 MVP backend dependencies
# Core web server
fastapi==0.115.6
uvicorn[standard]==0.32.1
websockets==13.1

# Config
python-dotenv==1.0.1

# LLM + STT — Groq hosts both Llama-3-8b-8192 and Whisper-large-v3
groq>=0.15.0

# TTS — ElevenLabs called directly over httpx (no extra SDK needed)
httpx==0.27.2
